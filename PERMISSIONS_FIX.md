# 🔧 حل مشكلة عدم ظهور صلاحيات النشر في Facebook Login

## ❌ المشكلة
صلاحيات النشر على Facebook لا تظهر عند تسجيل الدخول، مما يمنعك من الانتقال للـ Live Mode.

---

## ✅ الحل (5 خطوات فقط)

### الخطوة 1️⃣: تفعيل Live Mode للتطبيق

```
1. اذهب إلى: https://developers.facebook.com/apps
2. اختر تطبيقك "AI Content Scheduler"
3. اضغط على "Settings" → "Basic"
4. ابحث عن "App Status" في الأعلى
5. اضغط على الزر بجانب "App Status"
6. اختر: "Live"
7. اضغط "Confirm"
```

**النتيجة:** التطبيق الآن يطلب الصلاحيات كاملة ✅

---

### الخطوة 2️⃣: التأكد من إضافة Redirect URI

```
1. من نفس صفحة Settings → Basic
2. ابحث عن قسم: "Facebook Login"
3. اضغط "Settings" (تحت Facebook Login)
4. في حقل "Valid OAuth Redirect URIs"
5. أضف هذا الرابط:

   https://yourdomain.com/api/oauth/facebook/callback

   (استبدل yourdomain.com برابط موقعك الفعلي)

6. اضغط "Save Changes"
```

---

### الخطوة 3️⃣: التحقق من الصلاحيات المطلوبة

```
1. من نفس صفحة Settings → Basic
2. ابحث عن قسم: "Permissions"
3. اضغط على "Request or Remove"
4. تأكد من وجود هذه الصلاحيات:
   ✓ public_profile
   ✓ email
   ✓ pages_show_list
   ✓ pages_manage_posts
   ✓ pages_read_engagement
   ✓ user_pages
```

**ملاحظة:** إذا كانت ناقصة، اضغط "Request" وأضفها يدوياً.

---

### الخطوة 4️⃣: تأكد أنت Admin على الصفحة

```
1. افتح صفحتك على Facebook
2. اضغط ⚙️ (Settings)
3. اختر "Roles"
4. تأكد من أن دورك = "Admin"
   ❌ لا يمكنك النشر إذا كنت: Editor أو Moderator
   ✅ يجب أن تكون: Admin
```

---

### الخطوة 5️⃣: اختبر التطبيق

```
1. شغّل التطبيق:
   npm run dev

2. افتح: http://localhost:3000

3. اضغط على زر "Login with Facebook"

4. سجّل دخول بحسابك (أنت Admin على الصفحة)

5. يجب تظهر نافذة الصلاحيات مثل هذا:
   ┌─────────────────────────────────────┐
   │ التطبيق يطلب الوصول إلى:           │
   │                                       │
   │ ✓ البريد الإلكتروني                │
   │ ✓ الملف الشخصي العام              │
   │ ✓ صفحاتك                           │
   │ ✓ إدارة منشورات الصفحة             │
   │                                       │
   │ [اسمح] [إلغاء]                    │
   └─────────────────────────────────────┘

6. اضغط "اسمح" أو "Allow"

7. انتظر إعادة التوجيه للـ Dashboard
```

---

## 🔍 إذا لم تعمل؟

### المشكلة: الصلاحيات لا تزال لا تظهر

**الحل:**
1. تأكد أن التطبيق في **Live Mode** (الخطوة 1)
2. امسح بيانات المتصفح (Cache)
   - في Chrome: Ctrl+Shift+Delete
   - اختر جميع الفترات الزمنية
   - اضغط "حذف البيانات"
3. حاول تسجيل الدخول مرة أخرى

---

### المشكلة: "Invalid redirect URI"

**الحل:**
1. تأكد من الرابط في Facebook Settings:
   - يجب أن يكون **بالضبط** مثل ما في الكود:
   ```
   https://yourdomain.com/api/oauth/facebook/callback
   ```
2. لو في Development، أضف أيضاً:
   ```
   http://localhost:3000/api/oauth/facebook/callback
   ```

---

### المشكلة: "لا يوجد صفحات لهذا الحساب"

**الحل:**
1. تأكد أنت فعلاً Admin على صفحة Facebook
2. اطلب من مالك الصفحة أن يجعلك Admin
3. انتظر قليل (ممكن تأخذ 5 دقائق لتفعل)
4. حاول تسجيل الدخول مرة أخرى

---

## 📋 القائمة النهائية للتحقق

قبل الضغط على "Publish" للـ Live:

- [ ] التطبيق في **Live Mode**
- [ ] **Redirect URI** مضاف بشكل صحيح
- [ ] جميع **الصلاحيات** مفعلة (6 صلاحيات)
- [ ] أنت **Admin** على الصفحة
- [ ] **اختبار النشر** نجح بنجاح

---

## 🎉 بعد انتهاء كل هذا

أنت الآن جاهز:
- ✅ المستخدمون يمكنهم تسجيل الدخول عبر Facebook
- ✅ الصلاحيات تظهر بشكل صحيح
- ✅ يمكن النشر على صفحات Facebook
- ✅ يمكنك الانتقال للـ Live Mode

---

## ⏰ الوقت المتوقع

- خطوات التكوين: **5 دقائق**
- اختبار كل خطوة: **5 دقائق**
- **المجموع: 10 دقائق فقط**

---

## 💡 نصائح إضافية

### للمستخدمين الآخرين (بعد نشر التطبيق)

كل مستخدم جديد يجب أن:
1. يكون **Admin** على صفحة Facebook خاصة به
2. يسجل دخول من خلال **"Login with Facebook"**
3. يوافق على **الصلاحيات** (النافذة المنبثقة)
4. بعدها يقدر ينشر على صفحته

### الصلاحيات المطلوبة في الكود

موجودة في: `/src/app/api/oauth/facebook/login/route.ts`

```typescript
const scopes = [
  "public_profile",      // ملفك الشخصي
  "email",               // بريدك الإلكتروني
  "pages_show_list",     // عرض صفحاتك
  "pages_manage_posts",  // ⭐ نشر منشورات (الصلاحية الأساسية)
  "pages_read_engagement", // قراءة تعليقات وإعجابات
].join(",");
```

---

## 📞 في حالة العالق

1. تحقق من **الخطوة 1** (Live Mode) - هذه الأكثر نسياناً
2. امسح **Cache المتصفح** وحاول مرة أخرى
3. تأكد من **دورك على الصفحة** (Admin)
4. اتصل بـ **دعم Facebook**: https://developers.facebook.com/support

---

**آخر تحديث:** 2025-12-01
