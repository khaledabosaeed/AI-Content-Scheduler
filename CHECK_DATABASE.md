ف # 🔍 كيفية التحقق من قاعدة البيانات الحالية

## الطريقة 1: عبر Supabase Dashboard

1. افتح [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **Table Editor** من القائمة الجانبية
4. تحقق:
   - ✅ هل يوجد جدول اسمه `users`؟
   - ✅ هل يحتوي على الحقول: `id`, `email`, `password_hash`؟

---

## الطريقة 2: عبر SQL Editor

1. اذهب إلى **SQL Editor** في Supabase
2. نفذ هذا الكود:

```sql
-- التحقق من وجود جدول users
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name = 'users';
```

**النتيجة:**
- إذا ظهر `users` → الجدول موجود ✅
- إذا لم يظهر شيء → الجدول غير موجود ❌

---

## الطريقة 3: التحقق من الحقول الموجودة

إذا كان الجدول موجود، تحقق من حقوله:

```sql
-- عرض جميع الأعمدة في جدول users
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'users';
```

**يجب أن يحتوي على:**
```
column_name      | data_type
-----------------|------------------
id               | uuid
email            | character varying
password_hash    | text
name             | character varying
created_at       | timestamp
```

---

## ماذا أفعل بعد التحقق؟

### ✅ الحالة 1: الجدول موجود والحقول صحيحة
**لا تفعل شيء!** النظام سيعمل مباشرة.

---

### ⚠️ الحالة 2: الجدول موجود لكن ينقصه `password_hash`

نفذ هذا في SQL Editor:

```sql
-- إضافة حقل password_hash
ALTER TABLE users 
ADD COLUMN password_hash TEXT;

-- جعله مطلوب (NOT NULL) بعد ملء البيانات الموجودة
-- ALTER TABLE users 
-- ALTER COLUMN password_hash SET NOT NULL;
```

---

### ❌ الحالة 3: الجدول غير موجود

نفذ الملف `database-schema.sql` الذي أنشأناه:

```sql
-- أو نسخ محتوى database-schema.sql والصقه في SQL Editor
```

---

## 📝 ملاحظات مهمة

### إذا كان لديك مستخدمين حاليين في Supabase Auth:

```sql
-- نقل المستخدمين من auth.users إلى users الجديد
INSERT INTO public.users (id, email, name, password_hash, created_at)
SELECT 
  id,
  email,
  raw_user_meta_data->>'name' as name,
  'يجب_تحديث_كلمة_المرور' as password_hash,  -- ← سيحتاج المستخدمون لإعادة تعيين كلمة المرور
  created_at
FROM auth.users;
```

**⚠️ مشكلة:** كلمات المرور القديمة في `auth.users` مشفرة بطريقة Supabase ولا يمكن نقلها.

**الحل:**
- المستخدمون الحاليون يحتاجون "إعادة تعيين كلمة المرور"
- أو يمكنك الاحتفاظ بـ Supabase Auth للمستخدمين القدامى

---

## 🎯 التوصية

**السيناريو الأفضل:**

1. ✅ إنشاء جدول `users` جديد
2. ✅ المستخدمون الجدد يسجلون في النظام الجديد
3. ✅ (اختياري) المستخدمون القدامى يستخدمون Supabase Auth

**أو:**

1. ✅ نقل المستخدمين الحاليين للجدول الجديد
2. ✅ إرسال إيميل لهم لإعادة تعيين كلمة المرور
3. ✅ بعدها الجميع على النظام الجديد

