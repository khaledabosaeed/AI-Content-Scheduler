# 🎯 **فكرة المشروع العامة**

## **AI Content Scheduler - مجدول المحتوى بالذكاء الاصطناعي**

هذا المشروع عبارة عن **منصة ويب متكاملة لإدارة وجدولة المحتوى على وسائل التواصل الاجتماعي** مع دعم **الذكاء الاصطناعي** لمساعدتك في إنشاء المحتوى.

### **الفكرة الأساسية:**

تخيل أنك تدير عدة حسابات على وسائل التواصل الاجتماعي (فيسبوك، تويتر، إنستجرام، إلخ). بدلاً من أن تقوم بنشر المحتوى يدوياً كل مرة، هذا التطبيق يتيح لك:

1. **التسجيل والدخول بنظام أمان قوي** - حسابك الخاص المحمي
2. **استخدام الذكاء الاصطناعي** (Google Gemini) لإنشاء محتوى مبتكر من خلال محادثة تفاعلية
3. **حفظ المنشورات** التي أنشأتها عبر الذكاء الاصطناعي
4. **جدولة النشر** على منصات التواصل المختلفة في أوقات محددة
5. **ربط حساباتك** على منصات التواصل (سيتم ذلك عبر OAuth)
6. **النشر التلقائي** - التطبيق ينشر نيابة عنك في الأوقات المحددة

---

# 🏗️ **بنية المشروع (Architecture)**

المشروع يتبع معمارية **Feature-Sliced Design (FSD)** وهي بنية حديثة تنظم الكود حسب الميزات والوظائف، مما يجعل المشروع:
- سهل التوسع
- سهل الصيانة
- منظم ومرتب

### **المراحل الثلاث للمشروع:**

#### **المرحلة 1: نظام المصادقة (Authentication) ✅ مكتمل**
- تسجيل مستخدم جديد
- تسجيل الدخول
- حماية الصفحات
- إدارة الجلسات

#### **المرحلة 2: الدردشة مع الذكاء الاصطناعي ✅ مكتمل**
- محادثة تفاعلية مع AI
- إنشاء محتوى بناءً على طلبات المستخدم
- حفظ المحتوى المولد في قاعدة البيانات

#### **المرحلة 3: ربط المنصات والنشر التلقائي 🚧 قيد التطوير**
- ربط حسابات Facebook, Twitter, LinkedIn, Instagram
- جدولة المنشورات
- النشر التلقائي

---

# 💻 **التقنيات المستخدمة**

## **الواجهة الأمامية (Frontend)**

### 1. **Next.js 16** - الإطار الرئيسي
   - أحدث إصدار من Next.js
   - يستخدم **Turbopack** للتطوير السريع (بديل لـ Webpack)
   - **App Router** - نظام التوجيه الجديد
   - **Server Components** - مكونات تعمل على السيرفر لأداء أفضل

### 2. **React 19.2.0**
   - مكتبة بناء واجهة المستخدم
   - أحدث إصدار مع تحسينات الأداء

### 3. **TypeScript**
   - لضمان كتابة كود آمن من الأخطاء
   - يوفر التحقق من الأنواع

### 4. **Tailwind CSS 4.1.17**
   - للتصميم السريع والحديث
   - نظام مرن للـ Dark Mode
   - تصميم متجاوب (Responsive)

### 5. **إدارة النماذج (Forms)**
   - **React Hook Form** - لبناء النماذج بكفاءة
   - **Zod** - للتحقق من صحة البيانات المدخلة
   - **Yup** - مكتبة إضافية للتحقق

### 6. **إدارة الحالة (State Management)**
   - **TanStack Query (React Query)** - لإدارة البيانات من السيرفر
   - **Zustand** - لإدارة الحالة المحلية (مثل محادثة الـ AI)

### 7. **المكتبات المساعدة**
   - **Lucide React** - أيقونات حديثة
   - **React Toastify** - إشعارات جميلة للمستخدم
   - **next-themes** - للتبديل بين الوضع الفاتح والداكن

---

## **الخلفية (Backend)**

### 1. **Next.js API Routes**
   - يوفر Next.js نظام **Serverless Functions**
   - كل ملف في `/api` يصبح endpoint
   - مثال: `/api/auth/login`, `/api/chat`

### 2. **قاعدة البيانات - Supabase**
   - قاعدة بيانات **PostgreSQL** مستضافة على السحابة
   - توفر SDK جاهز للتعامل مع قاعدة البيانات
   - **جداول البيانات**:
     - `users` - معلومات المستخدمين
     - `posts` - المنشورات المحفوظة
     - `social_accounts` - الحسابات المرتبطة (فيسبوك، تويتر، إلخ)

---

## **الأمان (Security)**

### 1. **bcrypt** - تشفير كلمات المرور
   - يقوم بتشفير كلمات المرور قبل حفظها
   - **12 جولة من الـ Hashing** - معيار صناعي قوي
   - لا يتم حفظ كلمة المرور الأصلية أبداً

### 2. **JWT (JSON Web Tokens)**
   - لإنشاء **رموز (tokens)** آمنة للجلسات
   - كل مستخدم يحصل على token مشفر
   - يحتوي على معلومات المستخدم بشكل آمن
   - صلاحية الـ token: **7 أيام**

### 3. **HTTP-only Cookies**
   - تخزين الـ JWT في **Cookies محمية**
   - **HttpOnly** - لا يمكن الوصول إليها من JavaScript (حماية من XSS)
   - **Secure** - تعمل فقط على HTTPS في الإنتاج
   - **SameSite** - حماية من CSRF attacks

### 4. **Middleware**
   - يفحص كل طلب قبل السماح بالوصول
   - يحمي الصفحات الخاصة من غير المسجلين

---

## **الذكاء الاصطناعي (AI Integration)**

### **Google Generative AI (Gemini)**
   - يستخدم نموذج **gemini-2.5-flash**
   - للدردشة وإنشاء محتوى ذكي
   - يتم التفاعل معه عبر واجهة محادثة

---

## **أدوات التطوير (DevOps & Tools)**

### 2. **ESLint**
   - للتحقق من جودة الكود
   - يساعد في تجنب الأخطاء الشائعة

### 3. **Git**
   - لإدارة النسخ والتعاون

---

# 🔄 **كيف يعمل المشروع؟**

## **سيناريو الاستخدام:**

### 1. **المستخدم يسجل حساب جديد**
   - يدخل Email, Password, Name
   - يتم تشفير الباسورد بـ bcrypt
   - يتم حفظ البيانات في قاعدة Supabase
   - يتم إنشاء JWT token تلقائياً
   - يتم حفظ الـ token في Cookie
   - **تسجيل دخول تلقائي**

### 2. **المستخدم يستخدم AI Chat**
   - يفتح صفحة الدردشة
   - يكتب: "اكتب لي منشور عن فوائد القراءة"
   - يتم إرسال الطلب لـ Google Gemini API
   - يعود الرد الذكي
   - يمكن حفظ المحتوى في قاعدة البيانات

### 3. **المستخدم يربط حساباته الاجتماعية** (قريباً)
   - ينقر على "ربط Facebook"
   - يتم توجيهه لصفحة OAuth الخاصة بـ Facebook
   - بعد الموافقة، يتم حفظ Access Token
   - نفس العملية لـ Twitter, LinkedIn, Instagram

### 4. **المستخدم يجدول منشور**
   - يختار المنشور
   - يختار المنصات (Facebook + Twitter)
   - يحدد الوقت والتاريخ
   - يضغط "جدولة"

### 5. **النشر التلقائي**
   - التطبيق يفحص كل دقيقة
   - إذا حان وقت نشر منشور
   - يستخدم Access Tokens المحفوظة
   - ينشر على المنصات المحددة تلقائياً

---

# 🎨 **مميزات التصميم**

- **واجهة حديثة** - تصميم احترافي
- **Dark Mode** - وضع داكن مريح للعين
- **Responsive** - يعمل على الهاتف والحاسوب
- **سريع** - بفضل Turbopack و React 19

---

# 📊 **خلاصة التقنيات**

| **الطبقة** | **التقنية** | **السبب** |
|------------|-------------|-----------|
| Framework | Next.js 16 | Full-stack، SSR، API Routes |
| UI Library | React 19 | الأحدث، أفضل أداء |
| Language | TypeScript | Type Safety |
| Styling | Tailwind CSS 4 | سريع وحديث |
| Database | Supabase (PostgreSQL) | مجاني، سهل، قوي |
| Auth | JWT + bcrypt | آمن، معيار صناعي |
| AI | Google Gemini | ذكاء اصطناعي متقدم |
| State | TanStack Query + Zustand | فعّال وقوي |
| Forms | React Hook Form + Zod | سريع وموثوق |

---

# 🔐 **نظرة عامة على الأمان**

```
┌─────────────────────────────────────────────────────────┐
│                    Authentication Flow                   │
└─────────────────────────────────────────────────────────┘

Registration:
User Input → Password Hashing (bcrypt) → Save to DB → 
Generate JWT → Save to HTTP-only Cookie → Auto Login

Login:
User Input → Verify Password → Generate JWT → 
Save to HTTP-only Cookie → Redirect to Dashboard

Protected Routes:
Request → Middleware → Check Cookie → Verify JWT → 
Allow/Deny Access

Logout:
Clear Cookie → Redirect to Login
```

---

# 📂 **هيكل المشروع (Feature-Sliced Design)**

```
src/
├── app/                    # صفحات التطبيق و API Routes
│   ├── api/               # Backend Endpoints
│   │   ├── auth/          # نظام المصادقة
│   │   └── chat/          # الذكاء الاصطناعي
│   ├── landingPage/       # الصفحة الرئيسية
│   └── login/             # صفحة تسجيل الدخول
│
├── entities/              # الكيانات الأساسية
│   ├── post/             # كيان المنشورات
│   └── user/             # كيان المستخدم
│
├── features/              # الميزات الوظيفية
│   ├── auth/             # ميزات المصادقة
│   └── user/             # ميزات إدارة المستخدم
│
├── shared/                # الموارد المشتركة
│   ├── api/              # عملاء API
│   ├── libs/             # المكتبات الأساسية
│   │   ├── passwordHash.ts    # تشفير كلمات المرور
│   │   ├── jwt.ts             # إدارة JWT
│   │   ├── cookies.ts         # إدارة Cookies
│   │   └── supabase*.ts       # عملاء قاعدة البيانات
│   └── ui/               # مكونات الواجهة
│
└── widgets/               # المكونات المركبة
```

---

# 🚀 **الخطوات القادمة**

## **المرحلة 3 - التكامل مع وسائل التواصل:**

1. **OAuth Integration**
   - Facebook OAuth
   - Twitter OAuth
   - LinkedIn OAuth
   - Instagram OAuth

2. **جدولة المنشورات**
   - اختيار التاريخ والوقت
   - اختيار المنصات المستهدفة
   - معاينة المنشور

3. **النشر التلقائي**
   - Cron Jobs للفحص الدوري
   - Queue System للمنشورات المجدولة
   - معالجة الأخطاء والإعادة التلقائية

4. **Analytics & Reports**
   - إحصائيات المنشورات
   - معدلات التفاعل
   - تقارير الأداء

---

# 📚 **الوثائق الإضافية**

للمزيد من التفاصيل، راجع:

- [AUTH_SYSTEM_DOCUMENTATION.md](./AUTH_SYSTEM_DOCUMENTATION.md) - شرح نظام المصادقة بالتفصيل
- [WHAT_WAS_DONE_AR.md](./WHAT_WAS_DONE_AR.md) - ما تم إنجازه
- [DATABASE_SCHEMA.md](./DATABASE_SCHEMA.md) - هيكل قاعدة البيانات
- [PHASE_2_AI_CHAT_IMPLEMENTATION.md](./PHASE_2_AI_CHAT_IMPLEMENTATION.md) - تنفيذ الدردشة بالذكاء الاصطناعي
- [PHASE_3_SOCIAL_MEDIA_INTEGRATION_GUIDE.md](./PHASE_3_SOCIAL_MEDIA_INTEGRATION_GUIDE.md) - دليل التكامل مع وسائل التواصل

---

**تم إنشاء هذا المستند في:** 2025-11-25
